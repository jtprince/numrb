= Numrub

An experimental library aimed at providing support for numeric arrays that are manipulatable with C inside ruby using FFI-inliner.

== Examples

require 'numrub'

class MyNumrub < Numrub
  extend Inliner

end

nr = Numrub.new([1,2,3,4,5])
sum = nr.sum
 

== Copying

See LICENSE
